#finding second highest salary 
#common function 
Select 
max(salary) 
from emp 
where salary 
< (select max(salary) from emp)

#windows function 
#nth highest salary 
select name, salary from (select 
name, salary 
dense_rank() over ( order by salary ) as high_sal) t
where high_sal = 2
